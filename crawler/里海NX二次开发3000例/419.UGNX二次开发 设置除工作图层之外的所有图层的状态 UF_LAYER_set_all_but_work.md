### UGNX二次开发 设置除工作图层之外的所有图层的状态 UF_LAYER_set_all_but_work

#### 代码

```cpp
	//设置除工作图层之外的所有图层的状态 - 活动
	UF_LAYER_set_all_but_work(UF_LAYER_ACTIVE_LAYER);
```

```cpp
	//设置除工作图层之外的所有图层的状态 - 不活动
	UF_LAYER_set_all_but_work(UF_LAYER_REFERENCE_LAYER);
```

```cpp
	//设置除工作图层之外的所有图层的状态 - 参考
	UF_LAYER_set_all_but_work(UF_LAYER_INACTIVE_LAYER);
```

```cpp
	//设置除工作图层之外的所有图层的状态 为工作
	UF_LAYER_set_all_but_work(UF_LAYER_WORK_LAYER);
```

#### 代码解析
> 未找到解析内容

